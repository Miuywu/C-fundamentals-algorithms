#include "holberton.h"

/**
 * print_last_digit - prints last digit of int
 *
 * Return: value of last digit
 */

int _abs(int six)
{
	int a;

	if (six < 0)
	{
		a = six *(-1);
	}
	else
		a = six;
	return (a);
}
