#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 1 if c is upper, 0 otherwise
 * @argc: input
 * @argv: inout
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", *argv);
	return (0);
}
